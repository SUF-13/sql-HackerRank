DELIMITER //
CREATE PROCEDURE Pattern()
BEGIN
    DECLARE i INT DEFAULT 1;
    
    WHILE i<=20 DO
        SELECT REPEAT('* ', i);
        SET i=i+1;
    END WHILE;
END
//

DELIMITER ;
CALL Pattern();
