DELIMITER //

CREATE PROCEDURE printpattern()
BEGIN
    DECLARE i INT DEFAULT 0;
    SET i=20;
    
    WHILE i>=1 DO
        SELECT REPEAT('* ', i);
        SET i=i-1;
    END WHILE;
END
//

DELIMITER ;

CALL printpattern();
